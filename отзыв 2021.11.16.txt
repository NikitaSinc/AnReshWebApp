1 архитектура: 
	+ попробуй переделать редактирование/создание всего через диалоги, а не через отдельные страницы
	+ попробуй создать статический класс для параметров из *.config и используй его для доступа к настройкам - избавишься от размащанных magic numbers по коду
	+ унифицируй создание коннекции к бд
	+ обрабатывай ошибки
	? добавил, но что с ним делать? попробуй ввести базовый класс сущности с добавлением вариативности в интерфейсе IRepository<TEntity>

2 vue:
	? какие пути? используй глобальные переменные для путей - это спасет тебя при рефакторинге
	+ раздели store на модули по зонам ответственности - повысит читаемость
	+ обрабатывай ошибки запросов

3 по заданиям:
	- swagger
	- серверная фильтраиця/пагинация
	- скилы
	+ jwt: используй cookie, так вроще пробросить в запросе на веб апи
	? к чему относится? научись писать скрипты, которые тебе разом отдадут все нужные данные для фронта, не делай лишнего ненужного маппинга
	+ передавай модель в методы контроллера, чтобы не было огромного количества параметров
	
4 код
	- стиль должен быть единым - это повышает читаемость:
		наш стандарт
		* PascalCase для методов, классов, свойств, статичных переменных, констант
		* camelCase для локальных переменных
		* _camelCase для приватных глобальных переменных
		конец нашего стандарта
		* если используется snake case, тогда все 3 выше с подчеркиванием
	- форматирование